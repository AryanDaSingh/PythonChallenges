DECLARE HOURS: INTEGER
DECLARE MINUTES: STRING
DECLARE AMPM: STRING

INPUT HOURS
INPUT MINUTES
INPUT AMPM

AMPM ← UCASE(AMPM)

IF AMPM = "PM" THEN
    HOURS ← HOURS + 12
ENDIF

OUTPUT HOURS, ":", MINUTES
